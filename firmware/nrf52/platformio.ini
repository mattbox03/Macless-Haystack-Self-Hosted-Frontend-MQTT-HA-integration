[env:nrf52_dk]
platform = nordicnrf52@10.12.0
board = nrf52_dk
framework = zephyr

debug_tool = cmsis-dap
upload_protocol = cmsis-dap

; The firmware does not use UART, logging, LEDs, or buttons.
; A generic nRF52832 module can use the DK target because no board pins are used.
